# Railway usa Nixpacks; Node 20+ requerido (undici/File API).
[variables]
NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable", "pnpm install"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node dist/index.js"
